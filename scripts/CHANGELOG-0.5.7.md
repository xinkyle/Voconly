## [0.5.7] - 2026-09-03

### 修复
1. **修复升级安装后不自动启动**，升级完成后新版本会自动启动，无需手动双击快捷方式。

2. **修复默认设置不生效问题**，升级配置版本到 v3，确保新用户获得正确的默认值（预览窗口高度"低"、开机自启动开启）。

---

### Fixed
1. **Fixed auto-launch after update installation** - New version now launches automatically after update completes, no need to manually double-click the shortcut.

2. **Fixed default settings not applying** - Upgraded config version to v3, ensuring new users get correct default values (preview window height "low", auto-start enabled).